[pytest]
filterwarnings =
    ignore:Unverified HTTPS request is being made.*
markers =
    every: tests to be executed on every host
    master: tests to be executed only on master hosts
    distributed: tests to be executed only on distributed installations