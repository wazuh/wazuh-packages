# Elasticsearch nodes
elasticsearch-nodes:
  - name: node1
    ip:
      - <elastic_ip_addr_node1>

# Wazuh server nodes
wazuh-servers:
  - name: wazuh1
    ip:
      - <kibana_ip_addr>

# Kibana node
kibana:
  - name: kibana
    ip:
      - <wazuh_master_ip_addr>
